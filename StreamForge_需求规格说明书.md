# StreamForge 工业遥测数据处理系统需求规格说明书

| 文档属性 | 内容 |
| --- | --- |
| 项目名称 | StreamForge |
| 项目类型 | C/C++ 后端服务与命令行工具 |
| 目标平台 | Linux x86_64，兼容 macOS 开发环境 |
| 主要语言 | C++17；少量 C11 用于二进制协议编解码模块 |
| 指定编译器 | Clang 20（`clang-20`、`clang++-20`） |
| 预期规模 | 13,000～20,000 行有效代码 |
| 构建系统 | CMake 3.20 及以上 |
| 文档版本 | 1.1 |

## 1. 项目概述

StreamForge 是一个面向工厂边缘节点的工业设备遥测数据处理系统。系统接收来自温度、压力、振动、流量和功率传感器的数据，对数据进行格式解析、合法性校验、时间排序、去重、校准、单位换算、窗口聚合和异常检测，并将处理结果保存到本地数据库。运维人员可以通过 HTTP 接口或命令行查询设备状态、异常事件和统计结果，也可以导出 CSV 或 JSON 报表。

系统部署在资源有限且网络连接不稳定的边缘服务器上，因此必须支持有界内存、并发处理、断点恢复和优雅停机。项目不依赖外部消息队列或云服务，所有核心功能应能在单机环境中运行。

## 2. 建设目标

项目交付后应满足以下目标：

1. 支持 CSV、JSON Lines 和自定义 TLM 二进制文件三种遥测输入格式。
2. 建立从原始记录到标准化样本、聚合指标和异常事件的完整处理流水线。
3. 正确处理重复记录、乱序记录、缺失值、无效值、时间回拨和不完整文件。
4. 允许通过 YAML 配置设备、指标、校准参数、派生指标和异常规则。
5. 使用 SQLite 保存处理状态、标准化样本、聚合结果和异常事件。
6. 提供 HTTP 查询服务和完整的命令行管理能力。
7. 在进程异常退出后，从最近检查点继续处理，且不得重复生成业务结果。
8. 在普通四核开发机上稳定处理至少每秒 20,000 条输入记录。

## 3. 项目范围

### 3.1 本期范围

- 本地目录中的批量文件导入。
- 持续监听指定目录并处理新文件。
- 三种输入格式的流式解析。
- 遥测数据校验、校准、换算、去重和乱序重排。
- 缺失数据识别及有限插值。
- 滚动窗口和固定时间窗口聚合。
- 派生指标计算。
- 阈值、持续时间、变化率和组合条件异常检测。
- 异常事件合并、关闭、确认和查询。
- SQLite 持久化、检查点及数据保留清理。
- HTTP REST API、命令行工具和报表导出。
- 配置热加载、结构化日志、运行指标和审计记录。

### 3.2 非本期范围

- Web 管理界面。
- 用户注册、单点登录和复杂权限体系。
- 集群部署、跨节点复制和分布式一致性。
- Kafka、MQTT 等外部消息中间件接入。
- 机器学习模型训练或推理。
- 移动端应用。

## 4. 术语与核心概念

| 术语 | 定义 |
| --- | --- |
| 设备 Device | 产生遥测数据的物理设备，使用全局唯一字符串标识 |
| 指标 Metric | 被测量的物理量，如温度、压力、振动速度 |
| 原始记录 Raw record | 尚未通过业务校验和换算的输入记录 |
| 标准化样本 Sample | 完成校验、校准和单位换算后的单条数据 |
| 事件时间 Event time | 传感器记录数据时携带的时间 |
| 接收时间 Ingest time | StreamForge 读取记录的时间 |
| 水位线 Watermark | 系统认为不会再接收更早数据的事件时间边界 |
| 窗口 Window | 按时间范围划分的样本集合 |
| 派生指标 Derived metric | 由一个或多个基础指标计算得到的新指标 |
| 异常事件 Incident | 一条规则在特定设备上持续满足触发条件形成的事件 |
| 检查点 Checkpoint | 文件读取位置及处理进度的持久化状态 |

## 5. 总体业务流程

1. 发现输入目录中的候选文件，并判断文件是否写入完成。
2. 识别文件格式，逐条解析原始记录。
3. 对字段、设备、指标、时间和值执行基础校验。
4. 对样本执行去重，并在允许范围内按照事件时间重排。
5. 应用校准公式和单位换算，生成标准化样本。
6. 将样本送入缺失检测、窗口聚合和派生指标计算模块。
7. 使用标准化样本及聚合结果执行异常规则。
8. 在事务中保存样本、聚合结果、事件及检查点。
9. 通过 API、CLI 或报表向用户提供查询结果。
10. 文件完成处理后，将其移动到归档目录；失败文件进入隔离目录。

## 6. 技术约束

### 6.1 编程语言与标准

- 项目统一使用 Clang 20 编译器：C 源文件使用 `clang-20`，C++ 源文件使用 `clang++-20`。
- CMake 配置阶段必须检查编译器厂商和主版本号；不是 Clang 20 时应终止配置并给出明确提示。
- 业务主体使用 C++17，不得依赖编译器私有扩展。
- TLM 二进制协议的基础编解码层使用 C11，并提供清晰的 C/C++ 边界。
- 所有资源使用 RAII 管理；除与 C 接口交互外，不直接使用裸 `new` 和 `delete`。
- 公共时间类型统一使用 UTC 的微秒级 Unix 时间戳，内部优先使用 `std::chrono`。

标准构建命令如下：

```bash
CC=clang-20 CXX=clang++-20 cmake -S . -B build -DCMAKE_BUILD_TYPE=Release
cmake --build build --parallel
ctest --test-dir build --output-on-failure
```

### 6.2 建议依赖

允许使用以下第三方库，也可选择功能相当且维护活跃的替代库：

- SQLite3：持久化。
- yaml-cpp：配置解析。
- nlohmann/json：JSON 解析与序列化。
- cpp-httplib：HTTP 服务。
- spdlog：日志。
- Catch2：单元测试与集成测试。

第三方依赖应通过 CMake `FetchContent`、包管理器或系统库中的一种一致方式管理。不得将第三方源代码直接计入项目代码规模。

### 6.3 构建产物

构建后至少生成以下产物：

| 产物 | 用途 |
| --- | --- |
| `streamforge-server` | 目录监听、数据处理和 HTTP 服务主进程 |
| `streamforgectl` | 管理、查询、导入和报表导出命令行工具 |
| `libtlmcodec` | TLM 二进制协议 C11 静态库 |
| `streamforge-tests` | 自动化测试程序，可按标签选择测试集合 |

## 7. 功能需求

### 7.1 配置管理

#### FR-CFG-001 配置文件

系统必须从 YAML 文件加载配置。配置至少包括：

- 服务监听地址和端口。
- 输入、归档、隔离和报表目录。
- SQLite 数据库路径。
- 工作线程数、队列容量和批量提交大小。
- 最大允许乱序时间。
- 设备与指标定义。
- 校准、单位换算、派生指标和异常规则。
- 数据保留周期和日志策略。

#### FR-CFG-002 配置校验

启动前必须完成结构与语义校验。以下情况必须阻止启动并给出明确错误位置：

- 必填项缺失或字段类型错误。
- 设备、指标或规则 ID 重复。
- 派生指标引用不存在的指标。
- 派生指标之间存在循环依赖。
- 窗口大小、乱序范围、线程数或队列容量不合法。
- 规则表达式语法错误或引用不匹配的指标单位。

#### FR-CFG-003 配置热加载

收到 `SIGHUP` 或调用管理接口后，系统应重新读取配置。新配置必须先完整校验，再以原子方式替换当前配置。校准、单位、规则和保留策略可热加载；数据库路径、监听端口和线程数变更应被拒绝，并提示需要重启。

若加载失败，系统继续使用旧配置，不得进入部分更新状态。

### 7.2 输入文件发现与生命周期

#### FR-IN-001 批量导入

`streamforgectl import` 接收单个文件或目录。目录导入支持递归开关，并按文件路径的字典序提交任务。单个文件处理失败不得终止整个批次，命令结束时输出成功、跳过和失败数量。

#### FR-IN-002 目录监听

服务按配置的扫描间隔轮询输入目录。只有满足以下任一条件的文件才可开始处理：

- 文件名以 `.ready` 结尾；处理时根据去除 `.ready` 后的扩展名识别格式。
- 文件大小和最后修改时间在连续两次扫描中均未变化，且静默时间达到配置值。

临时文件、隐藏文件和不支持的扩展名应忽略并记录调试日志。

#### FR-IN-003 文件身份

每个输入文件使用以下信息构造身份：规范化绝对路径、文件大小、最后修改时间和前 64 KiB 内容的 SHA-256。系统必须记录文件身份和处理状态。同一身份再次出现时应跳过；路径相同但内容变化时应视为新版本。

#### FR-IN-004 归档与隔离

- 成功完成的文件移动到按 `YYYY/MM/DD` 划分的归档目录。
- 无法解析或超过错误率阈值的文件移动到隔离目录。
- 跨文件系统移动应退化为复制、校验后删除源文件。
- 目标文件已存在时，在文件名后添加短哈希，不得覆盖已有文件。
- 隔离文件旁必须生成同名 `.error.json`，记录错误码、摘要和最多 100 条样例错误。

### 7.3 输入格式

#### FR-FMT-001 标准逻辑字段

三种输入格式最终映射到以下原始记录：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `device_id` | string | 是 | 1～64 字符，允许字母、数字、`-`、`_`、`.` |
| `metric` | string | 是 | 指标代码 |
| `event_time` | timestamp | 是 | UTC 或带时区的 ISO 8601，亦可为 Unix 毫秒 |
| `value` | number/null | 是 | 双精度浮点数或空值 |
| `unit` | string | 是 | 输入单位 |
| `quality` | integer | 否 | 0 为正常，1 为可疑，2 为设备错误；默认 0 |
| `sequence` | uint64 | 否 | 设备侧递增序号 |
| `tags` | map<string,string> | 否 | 单项最长 128 字符，最多 16 项 |

#### FR-FMT-002 CSV

- 编码为 UTF-8，可带 BOM。
- 第一行必须为表头，字段顺序不限。
- 分隔符支持逗号或分号，由配置指定或自动识别。
- 支持 RFC 4180 风格的引号和转义。
- 空行忽略；以 `#` 开头的非引号行视为注释。
- `tags` 使用 `key=value|key2=value2` 形式。
- 单行最大 1 MiB，超过限制时记录格式错误并跳过该行。

CSV 示例：

```csv
device_id,metric,event_time,value,unit,quality,sequence,tags
pump-07,bearing_temp,2026-08-01T09:15:30.125+08:00,76.4,C,0,18342,line=A|zone=east
```

#### FR-FMT-003 JSON Lines

- 每行是一个完整 JSON 对象。
- 不允许顶层数组。
- 未知字段默认保留在扩展字段中，但名称不得与标准字段冲突。
- 单行最大 1 MiB；嵌套深度最大 16。
- 数字不允许为 JSON 字符串，不接受 `NaN` 或 `Infinity`。

#### FR-FMT-004 TLM 二进制格式

TLM 使用小端编码，文件由文件头和若干数据帧组成。

文件头固定 24 字节：

| 偏移 | 长度 | 字段 | 说明 |
| ---: | ---: | --- | --- |
| 0 | 4 | magic | ASCII `TLM1` |
| 4 | 2 | version | 当前为 1 |
| 6 | 2 | header_size | 固定为 24 |
| 8 | 8 | created_at_ms | UTC Unix 毫秒 |
| 16 | 4 | flags | 当前必须为 0 |
| 20 | 4 | header_crc32 | 前 20 字节 CRC32 |

每个数据帧由 16 字节帧头、负载和 4 字节负载 CRC32 构成：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| sync | uint16 | 固定 `0xA55A` |
| frame_type | uint8 | 1=数据，2=设备元数据，3=心跳 |
| flags | uint8 | bit0 表示负载使用 zlib 压缩 |
| payload_size | uint32 | 解压前负载长度不得超过 4 MiB |
| sequence | uint64 | 文件内严格递增 |

数据帧负载采用 TLV 编码。C11 编解码库必须：

- 对所有长度和整数运算进行越界检查。
- 不假设输入缓冲区对齐。
- 能跳过未知 TLV 类型。
- 对截断、CRC 错误、序号回退和非法压缩数据返回不同错误码。
- 在损坏帧后最多向前扫描 1 MiB 寻找下一个同步字；恢复后必须标记数据质量为可疑。

### 7.4 校验、标准化与错误处理

#### FR-VAL-001 基础校验

每条记录依次执行以下检查：

1. 字段长度和字符集。
2. 设备及指标是否存在。
3. 时间是否可解析，是否超出允许范围。
4. `value` 是否为有限数值或允许的空值。
5. 输入单位是否在该指标的可转换单位集合中。
6. `quality` 是否为合法枚举值。
7. 标签数量、名称和长度是否合规。

严重格式错误跳过当前记录；业务校验错误也跳过记录，但计入文件错误率。错误率超过配置阈值时停止处理并隔离文件。

#### FR-VAL-002 时间规则

- 所有带时区时间转换为 UTC。
- 未带时区的 ISO 8601 时间使用设备配置的时区解释。
- 夏令时导致的不存在时间应拒绝；重复时间应按配置选择较早或较晚偏移。
- 事件时间晚于接收时间超过 10 分钟，或早于接收时间超过配置的历史导入范围时，记录无效。
- 闰秒 `:60` 归一化到下一分钟，并附加 `leap_second=true` 标签。

#### FR-VAL-003 校准

每个设备指标可配置分段线性校准：

`calibrated = slope * raw + intercept`

校准段按原始值的闭开区间 `[min, max)` 匹配。区间不得重叠。若无匹配区间，根据配置选择拒绝样本或使用原值。校准后再执行单位换算和有效范围检查。

#### FR-VAL-004 单位换算

至少内置以下换算：

- 摄氏度、华氏度和开尔文。
- Pa、kPa、MPa 和 bar。
- mm/s、cm/s 和 m/s。
- W、kW 和 MW。
- L/min、m³/h 和 m³/s。

每个指标指定规范单位，数据库只保存规范单位的值。换算函数必须检测溢出及物理上无效的值，例如低于绝对零度的温度。

### 7.5 去重与乱序处理

#### FR-ORD-001 去重键

有 `sequence` 时，去重键为 `(device_id, metric, sequence)`；无 `sequence` 时，去重键为 `(device_id, metric, event_time_us, normalized_value)`。浮点值必须使用确定的二进制表示或规范化文本参与哈希，不得使用带容差比较作为唯一键。

去重状态至少覆盖最近 24 小时，并在进程重启后仍有效。

#### FR-ORD-002 乱序缓冲

- 每个设备独立维护最大已见事件时间。
- 水位线为 `max_event_time - allowed_lateness`。
- 水位线之前的样本按事件时间和序号稳定输出。
- 缓冲区必须有记录数和字节数双重上限。
- 达到上限时提前推进水位线，并为受影响样本增加 `forced_flush=true` 标签。
- 迟于水位线到达的数据仍保存为样本，但默认不修改已封闭窗口；若配置启用修正，则重新计算受影响窗口并增加结果版本号。

### 7.6 缺失检测与插值

#### FR-MIS-001 缺失检测

设备指标可配置期望采样周期和允许抖动。当相邻有效样本间隔超过 `period + jitter` 时生成缺失区间。缺失区间应保存开始时间、结束时间和预计缺失点数。

#### FR-MIS-002 插值

系统支持以下策略：

- `none`：不插值。
- `previous`：使用前值填充。
- `linear`：在前后有效样本之间线性插值。

仅当缺口长度不超过指标配置的 `max_gap` 时允许插值。设备错误质量的样本不得作为插值端点。插值样本必须标记 `synthetic=true`，并可通过查询参数排除。

### 7.7 窗口聚合

#### FR-AGG-001 固定时间窗口

支持 1 分钟、5 分钟、15 分钟、1 小时和 1 天的 UTC 对齐窗口，也支持配置的整数秒窗口。每个窗口计算：

- 样本数、有效样本数、缺失数和异常质量数。
- 最小值、最大值、算术平均值、总和。
- 总体标准差。
- 第一个值、最后一个值及二者时间。
- P50、P95 和 P99 近似分位数。

分位数允许使用固定内存的近似算法，但相对于精确值的误差不得超过该窗口值域的 1%。

#### FR-AGG-002 滚动窗口

异常规则可引用最近 N 个有效样本或最近一段时间的滚动平均、最小值、最大值、标准差和变化率。滚动状态必须增量维护，不能每次扫描数据库中的全部历史样本。

#### FR-AGG-003 窗口版本

聚合结果的唯一键为 `(device_id, metric, window_type, start_time, version)`。正常结果版本为 1；迟到数据修正时创建新版本，并标记旧版本被取代。默认查询只返回最新版本。

### 7.8 派生指标

#### FR-DER-001 表达式

派生指标使用受限表达式语言，支持：

- 四则运算、取模和括号。
- 比较、逻辑与、逻辑或、逻辑非。
- 条件表达式 `if(condition, a, b)`。
- `abs`、`min`、`max`、`clamp`、`sqrt`、`pow`。
- 同一设备上其他指标的当前值。
- `avg(metric, duration)`、`delta(metric, duration)` 和 `rate(metric, duration)`。

表达式需在配置加载时解析为 AST，并完成引用、类型和单位检查。运行时不得使用 `eval`、脚本解释器或启动外部进程。

#### FR-DER-002 计算语义

- 一个派生指标可依赖其他派生指标，但依赖图必须无环。
- 同一时间点缺少输入时，按配置选择输出空值、使用最近值或跳过计算。
- 最近值的最大年龄必须可配置。
- 除零、负数开方、非有限结果和数值溢出必须转换为明确的计算错误，不得导致服务崩溃。
- 派生样本与原始样本使用相同的数据模型，并标记来源表达式版本。

派生指标示例：

```yaml
derived_metrics:
  - id: pump_efficiency
    unit: percent
    expression: "clamp(flow_rate * outlet_pressure / max(input_power, 0.001) * 0.027, 0, 100)"
    missing_policy: recent
    max_input_age: 5s
```

### 7.9 异常检测规则

#### FR-RULE-001 规则类型

系统至少支持以下规则：

1. **阈值规则**：值高于、低于或位于指定区间外。
2. **持续时间规则**：条件连续满足指定时长或样本数后触发。
3. **变化率规则**：单位时间变化率超过阈值。
4. **缺失规则**：指定时间内未收到有效样本。
5. **组合规则**：以表达式组合多个基础或派生指标。

每条规则包含唯一 ID、版本、适用设备标签、严重级别、触发条件、恢复条件、持续时间、冷却期和事件合并间隔。

#### FR-RULE-002 状态机

规则实例按设备维护以下状态：

- `NORMAL`：条件未满足。
- `PENDING`：条件已满足，但持续时间尚未达到。
- `FIRING`：已产生开放事件。
- `RECOVERING`：恢复条件已满足，但恢复持续时间尚未达到。
- `COOLDOWN`：事件关闭后处于抑制期。

状态转换必须基于事件时间。同一规则和设备在任意时刻最多存在一个开放事件。

#### FR-RULE-003 事件管理

异常事件至少包含：事件 ID、规则及版本、设备、严重级别、开始时间、最近命中时间、结束时间、当前状态、峰值、命中次数、确认信息和上下文快照。

- 短暂恢复后在合并间隔内再次触发，应重开原事件并增加重开次数。
- 用户可以确认事件并附加备注；确认不改变规则状态。
- 规则恢复时自动关闭事件。
- 规则被删除时，相关开放事件以 `RULE_REMOVED` 原因关闭。
- 对同一输入重放时不得重复创建事件。

### 7.10 持久化与事务

#### FR-DB-001 数据库结构

SQLite 至少包含以下逻辑实体：

- `devices`
- `metrics`
- `source_files`
- `checkpoints`
- `samples`
- `missing_intervals`
- `aggregates`
- `rules`
- `rule_states`
- `incidents`
- `incident_history`
- `audit_log`

应提供带版本号的数据库迁移机制。应用启动时自动执行向前迁移；不要求自动降级。

#### FR-DB-002 事务边界

样本批次、由该批次产生的聚合结果、规则状态、事件变化和检查点更新必须在同一事务中提交。进程在事务任意位置退出后，重启不得出现检查点已推进但业务结果缺失的情况。

#### FR-DB-003 写入与查询并发

- 数据库启用 WAL 模式和外键约束。
- 使用单写者模型或其他能够避免写锁竞争的明确方案。
- 写入繁忙时采用有上限的指数退避，不得无限重试。
- 长查询应支持超时或取消，不得永久阻塞写入流水线。

#### FR-DB-004 数据保留

原始样本、聚合结果、已关闭事件和审计日志可分别配置保留天数。清理任务分批删除，单个事务删除行数有上限。开放事件、当前规则状态和活动检查点不得被清理。

### 7.11 检查点与恢复

#### FR-REC-001 文件检查点

文本文件检查点记录安全提交的字节偏移和下一逻辑行号；TLM 文件记录下一帧偏移和上一帧序号。检查点只能在相关业务事务提交后推进。

#### FR-REC-002 恢复流程

服务启动时：

1. 将状态为 `PROCESSING` 的文件标记为待恢复。
2. 校验当前文件身份是否与记录一致。
3. 身份一致时从检查点继续。
4. 文件变化时终止恢复，将原任务标记为 `SOURCE_CHANGED`，并将当前文件作为新版本处理。
5. 对已经完成但尚未归档的文件补做归档操作。

#### FR-REC-003 优雅停机

收到 `SIGTERM` 或 `SIGINT` 后停止接收新文件，等待当前批次完成或达到停机超时。超时后回滚未提交事务、保存可安全保存的状态并退出。主进程不得使用 `_exit` 绕过资源清理。

### 7.12 HTTP API

#### FR-API-001 通用约定

- API 前缀为 `/api/v1`。
- 请求和响应使用 UTF-8 JSON；报表下载除外。
- 时间字段输出为 UTC ISO 8601，保留微秒。
- 错误响应统一包含 `code`、`message`、`request_id` 和可选 `details`。
- 列表接口使用游标分页，默认 100 条，最大 1000 条。
- 支持通过 `X-Request-Id` 传入请求标识；未提供时由服务生成。

#### FR-API-002 接口清单

| 方法 | 路径 | 功能 |
| --- | --- | --- |
| GET | `/health` | 进程存活状态，不访问数据库 |
| GET | `/ready` | 数据库、工作队列和配置就绪状态 |
| GET | `/api/v1/devices` | 查询设备列表及最新状态 |
| GET | `/api/v1/devices/{id}` | 查询单个设备详情 |
| GET | `/api/v1/samples` | 按设备、指标和时间范围查询样本 |
| GET | `/api/v1/aggregates` | 查询窗口聚合结果 |
| GET | `/api/v1/incidents` | 按状态、级别、设备、规则和时间查询事件 |
| GET | `/api/v1/incidents/{id}` | 查询事件及状态变更历史 |
| POST | `/api/v1/incidents/{id}/ack` | 确认事件并记录备注 |
| POST | `/api/v1/admin/reload` | 重新加载配置 |
| POST | `/api/v1/admin/import` | 提交服务器本地路径的导入任务 |
| GET | `/api/v1/admin/jobs/{id}` | 查询导入或报表任务状态 |
| GET | `/metrics` | 输出 Prometheus 文本格式运行指标 |

#### FR-API-003 查询限制

- 样本查询的时间范围默认不超过 24 小时，管理员配置可调整最大值。
- 未提供时间范围时拒绝查询样本和聚合结果。
- 所有筛选参数使用绑定变量，不得拼接 SQL。
- 导出超过 10,000 行时必须转为后台任务。

#### FR-API-004 管理接口认证

普通只读接口默认仅绑定本地或受信网络。管理接口要求 `Authorization: Bearer <token>`。令牌从环境变量或权限为 `0600` 的文件加载，禁止在日志中输出令牌。使用恒定时间比较验证令牌。

### 7.13 命令行工具

`streamforgectl` 至少提供：

```text
streamforgectl validate-config --config <file>
streamforgectl import <path> [--recursive] [--wait]
streamforgectl status
streamforgectl devices list [--tag key=value]
streamforgectl samples query --device <id> --metric <id> --from <time> --to <time>
streamforgectl incidents list [--state open] [--severity high]
streamforgectl incidents ack <incident-id> --by <name> --comment <text>
streamforgectl report create --type <summary|incident|quality> --from <time> --to <time> --format <csv|json>
streamforgectl replay --file <path> [--rules-only] [--dry-run]
streamforgectl db migrate
streamforgectl db check
```

命令行应提供 `--help`，参数错误退出码为 2，业务失败为 1，成功为 0。支持人类可读输出和 `--output json`，JSON 模式下不得混入进度文本。

### 7.14 报表

#### FR-RPT-001 报表类型

- **运行摘要**：设备数、样本数、错误数、缺失率和各指标统计。
- **异常事件报表**：按严重级别、规则和设备汇总，并包含事件明细。
- **数据质量报表**：重复、迟到、缺失、插值、格式错误和设备错误数据统计。

#### FR-RPT-002 输出要求

- 支持 CSV 和 JSON。
- CSV 防止以 `=`, `+`, `-`, `@` 开头的文本被电子表格解释为公式。
- 报表先写入临时文件，刷新并关闭成功后再原子重命名。
- 用户取消或生成失败时清理临时文件。
- 报表记录配置版本、查询范围、生成时间和数据截止时间。

### 7.15 日志、指标与审计

#### FR-OBS-001 日志

日志支持文本和 JSON 两种格式，包含时间、级别、线程、组件、请求 ID、文件 ID 和消息。日志轮转策略可配置。解析错误中的原始数据最多记录 256 字节，并移除控制字符；认证信息不得写入日志。

#### FR-OBS-002 运行指标

至少输出：

- 读取、接受、拒绝、重复、迟到和插值记录数。
- 各处理阶段队列深度和累计等待时间。
- 文件处理成功、隔离和恢复次数。
- 数据库事务耗时、回滚和繁忙重试次数。
- 开放事件数和各规则触发次数。
- HTTP 请求数、状态码和延迟直方图。

#### FR-OBS-003 审计日志

配置重载、事件确认、手工导入、重放、数据库迁移和数据清理必须记录审计日志。审计记录包含操作、操作者、时间、目标、结果和请求 ID，不允许通过常规清理接口修改。

## 8. 并发与背压设计要求

### 8.1 处理阶段

实现至少包含以下并发阶段：

1. 文件发现与任务调度。
2. 文件读取与格式解析。
3. 标准化、排序和派生计算。
4. 聚合与规则计算。
5. 数据库批量写入。

阶段间使用有界阻塞队列。队列满时上游等待或降低读取速度，不得无限缓存。关停、异常和取消时，所有等待线程必须能够被唤醒。

### 8.2 顺序保证

- 同一设备的样本在越过水位线后必须按事件时间处理。
- 相同事件时间按 `sequence`、文件提交顺序和文件内位置确定稳定顺序。
- 不同设备允许并行处理。
- 同一文件的检查点提交顺序不得倒退。

### 8.3 错误传播

工作线程的未处理异常必须被捕获并传递给任务协调器。单个文件错误不得终止整个服务；数据库不可恢复错误、配置核心不一致或内部不变量破坏应使服务进入非就绪状态并有序退出。

## 9. 数据模型要求

### 9.1 标准化样本

标准化样本至少包含：

```text
sample_id            全局唯一 ID
device_id            设备 ID
metric_id            指标 ID
event_time_us         事件时间
ingest_time_us        接收时间
value                 规范单位数值，可空
quality               数据质量
source_file_id        来源文件
source_position       行号或帧偏移
sequence              可空设备序号
flags                 duplicate/late/synthetic/forced_flush 等位标志
config_version        处理时使用的配置版本
expression_version    派生样本的表达式版本，可空
tags_json             规范化标签
```

### 9.2 标识符

- 文件、任务、事件等跨进程对象使用 UUID v4 或等价的 128 位随机 ID。
- 数据库整数主键可以内部使用，但不得作为 API 中长期稳定的唯一标识。
- 规则、设备和指标 ID 区分大小写，并在配置中保持稳定。

### 9.3 数值规则

- SQLite 中的数值使用 REAL 保存，并另存 `is_null` 或使用 SQL NULL 表示缺失。
- 禁止将 NaN 和无穷值持久化为正常样本。
- 对金额无需求，不得为了所有物理量统一引入十进制定点类型。
- 聚合计算应使用能够降低累计误差的算法，例如 Welford 方差和补偿求和。

## 10. 非功能需求

### 10.1 性能

在 4 核 CPU、8 GiB 内存、SSD、SQLite 位于本地磁盘的环境中：

- 对 10 个设备、20 个指标、10% 乱序、1% 重复的 JSONL 数据，持续吞吐不低于 20,000 条/秒。
- 处理 1,000 万条记录时，常驻内存不超过 512 MiB，且不随已处理数据量线性增长。
- 100 万条样本数据库中，查询单设备单指标 24 小时数据的 P95 响应时间不超过 500 ms，返回行数上限为 1,000。
- 收到正常停机信号后，在无数据库故障时 30 秒内退出。

性能测试必须提供可重复的数据生成器和测试命令。

### 10.2 可靠性

- 单条错误数据、损坏文件或客户端断开不得导致服务崩溃。
- 对相同配置和输入重复执行，除随机 ID、接收时间和日志时间外，业务结果应一致。
- 文件处理、检查点和事件生成必须具备幂等性。
- 关键数据写入失败时不得静默丢弃。
- 数据库损坏检测应返回明确诊断，不得自动删除或重建数据库。

### 10.3 安全性

- 所有文件路径必须规范化，并限制在配置允许的目录中，防止路径穿越。
- 不跟随输入目录中的符号链接，除非显式配置且目标仍位于允许目录。
- 解析器不得依据输入声明分配无上限内存。
- HTTP 请求体默认上限 1 MiB，报表下载除外。
- 所有 SQL 使用预编译语句和绑定参数。
- 外部输入不得直接作为格式化字符串、shell 命令或文件名使用。
- 错误消息不得泄露认证令牌、完整环境变量或无关文件内容。

### 10.4 可维护性

- 公共头文件保持最小依赖，避免循环包含。
- 业务层不得直接依赖 HTTP 框架类型或 SQLite C API。
- 格式解析、表达式、规则状态机、持久化和 API 通过明确接口隔离。
- 错误处理使用统一的错误码及上下文机制，不以日志替代错误返回。
- 所有公共类、接口和非直观业务规则需有文档注释。
- 代码通过 `clang-format-20`，并在 CI 中使用 `clang-tidy-20` 执行静态检查。

### 10.5 工具链与兼容性

- Linux 正式构建、测试和发布统一使用 Clang 20，不要求兼容 GCC 或其他 Clang 主版本。
- macOS 仅作为开发环境兼容目标；正式验收以 Linux x86_64 上的 Clang 20 构建结果为准。
- 支持 Debug、Release 和 AddressSanitizer/UndefinedBehaviorSanitizer 构建。
- 数据库模式升级至少保证从前一个版本迁移到当前版本。
- 配置文件包含 `schema_version`；未知主版本必须拒绝，未知可选字段应给出警告。

## 11. 建议模块划分

建议采用以下目录结构。可以在不改变职责边界的前提下调整名称：

```text
streamforge/
├── CMakeLists.txt
├── cmake/
├── config/
│   └── example.yaml
├── include/streamforge/
│   ├── core/
│   ├── ingest/
│   ├── processing/
│   ├── rules/
│   ├── storage/
│   ├── api/
│   └── report/
├── src/
│   ├── core/
│   ├── config/
│   ├── ingest/
│   │   ├── csv/
│   │   ├── jsonl/
│   │   └── tlm/
│   ├── processing/
│   ├── rules/
│   ├── storage/
│   ├── api/
│   ├── report/
│   ├── server_main.cpp
│   └── ctl_main.cpp
├── c/
│   └── tlmcodec/
├── migrations/
├── tests/
│   ├── unit/
│   ├── integration/
│   ├── fixtures/
│   └── fuzz/
├── tools/
│   └── data_generator/
├── docs/
└── README.md
```

模块职责如下：

| 模块 | 主要职责 |
| --- | --- |
| core | 时间、错误、ID、线程队列、生命周期和通用类型 |
| config | YAML 加载、语义校验、配置快照和热加载 |
| ingest | 文件发现、身份计算、格式识别、解析、归档和隔离 |
| tlmcodec | C11 二进制帧与 TLV 编解码、CRC 和错误恢复 |
| processing | 校准、单位、去重、乱序、插值、窗口和派生指标 |
| rules | 表达式 AST、规则编译、状态机和事件生成 |
| storage | SQLite 封装、迁移、事务、仓储、检查点和清理 |
| api | HTTP 路由、参数校验、分页、认证和响应映射 |
| report | 查询快照、CSV/JSON 流式输出和后台任务 |
| cli | 参数解析、远程 API 客户端和本地维护命令 |

## 12. 代码规模与工作量边界

有效代码指项目自有的 `.c`、`.h`、`.cpp`、`.hpp` 文件中的非空、非纯注释行，不包括第三方库、生成代码、构建目录、迁移 SQL、测试数据和文档。

| 部分 | 建议有效代码量 |
| --- | ---: |
| 核心类型、配置与通用设施 | 1,300～1,800 |
| 三种输入格式及文件生命周期 | 1,800～2,500 |
| 数据处理、窗口和派生指标 | 2,000～2,800 |
| 表达式、规则状态机和事件 | 1,500～2,200 |
| SQLite、迁移和检查点 | 1,200～1,800 |
| HTTP API、CLI 和报表 | 1,600～2,400 |
| 数据生成器及其他项目代码 | 300～500 |
| 自动化测试 | 3,300～4,500 |
| **合计** | **13,000～18,500** |

最终交付目标为 13,000～20,000 行，表中区间仅用于规划，允许各模块上下浮动。若实现接近上限，应优先减少重复封装或非必要适配，不得通过复制代码、生成无意义样板或拆分简单函数扩充行数。

## 13. 测试要求

### 13.1 单元测试

至少覆盖：

- CSV 引号、换行、BOM、空字段和超长行。
- JSON 类型错误、深度限制和未知字段。
- TLM 大小端读取、未对齐缓冲区、CRC、截断、溢出和同步恢复。
- 时区、夏令时边界、时间回拨和闰秒归一化。
- 全部单位换算及边界值。
- 分段校准的区间边界和配置重叠。
- 去重键、乱序水位线、缓冲区强制刷新和迟到修正。
- 缺失区间和三种插值策略。
- 窗口边界、方差、分位数和结果版本。
- 表达式优先级、类型、单位、短路、缺失值和数值错误。
- 规则状态机的每个状态转换、冷却和重开。
- SQL 事务回滚、繁忙重试和迁移。
- API 参数校验、游标和令牌认证。

核心模块行覆盖率不低于 80%，分支覆盖率不低于 70%。覆盖率仅作为最低要求，关键边界必须有明确断言。

### 13.2 集成测试

至少提供以下端到端场景：

1. 导入三种格式并查询一致的标准化结果。
2. 文件中含 5% 错误记录但未超过阈值，正确记录正常提交。
3. 文件错误率超过阈值，事务回滚且文件进入隔离目录。
4. 处理中强制终止进程，重启后从检查点恢复且无重复结果。
5. 多文件并行导入同一设备乱序数据，最终窗口和事件保持确定性。
6. 配置热加载成功，新样本使用新版本，历史数据不被改写。
7. 配置热加载失败，旧规则继续工作。
8. 迟到数据触发窗口修正，默认查询返回最新版本。
9. 数据库短暂繁忙后恢复，流水线继续且无数据丢失。
10. 优雅停机期间 HTTP 服务停止接收管理任务，已提交批次完成。

### 13.3 属性测试与模糊测试

- 对 TLM 帧解析器和 TLV 解析器提供 libFuzzer 入口。
- 对表达式解析器提供随机 token 输入测试。
- 对单位换算验证往返误差性质。
- 对去重与重放验证幂等性质。
- 模糊测试发现的崩溃样本必须可作为普通回归测试运行。

### 13.4 性能测试

数据生成器可控制设备数、指标数、记录数、重复率、乱序率、错误率和随机种子。性能测试输出吞吐、P50/P95/P99 延迟、峰值内存、数据库大小和各阶段队列峰值。

## 14. 验收标准

### 14.1 功能验收

- 本文标记为必须的功能全部实现。
- 示例配置可直接启动服务并处理随项目提供的示例数据。
- 三种格式对等字段得到一致的标准化结果。
- API 和 CLI 能完成设备、样本、聚合、事件和任务的主要操作。
- 配置错误、输入错误和数据库错误均返回可定位原因。

### 14.2 数据正确性验收

- 使用固定测试集，将标准化样本、窗口聚合和事件结果与预期文件逐项比对。
- 同一输入重放三次，数据库中的样本、聚合和事件业务数量不增加。
- 在至少五个随机事务位置模拟进程退出，恢复后的最终结果与不中断运行一致。
- 跨 UTC 日期、月末、闰年和夏令时切换的窗口边界正确。

### 14.3 工程质量验收

- 使用 Clang 20 完成 Release、Debug 构建，开启 `-Wall -Wextra -Wpedantic`，项目自有代码无警告。
- `clang-format-20` 格式检查和 `clang-tidy-20` 静态检查通过；检查范围至少覆盖项目自有的 C/C++ 源文件和公共头文件。
- AddressSanitizer 和 UndefinedBehaviorSanitizer 下完整测试通过。
- 自动化测试覆盖率达到第 13.1 节要求。
- 无未说明的全局可变状态，无后台线程在测试结束后遗留。
- 所有数据库语句、文件句柄、网络连接和线程能够在错误路径上正确释放。
- `README.md` 包含构建、配置、运行、示例导入、查询、测试和故障排查步骤。

### 14.4 性能验收

- 满足第 10.1 节吞吐、内存、查询延迟和停机时间要求。
- 在持续 2 小时压力测试中无崩溃、死锁、线程泄漏或持续内存增长。
- 队列饱和时能够产生背压，输入端速度下降而非内存无界增长。

## 15. 交付物

最终应交付：

1. 完整 C/C++ 源代码和 CMake 构建文件。
2. 数据库迁移脚本和示例配置。
3. 可用于三种格式的示例输入文件。
4. 自动化单元测试、集成测试、模糊测试入口和性能测试工具。
5. `README.md`、架构说明、配置参考、API 文档和 TLM 协议说明。
6. Linux 下的构建及测试脚本，或功能等价的 CI 配置。
7. 一份验收记录，列出已实现功能、测试结果、性能结果和已知限制。

## 16. 实施顺序

建议按以下里程碑完成，每个里程碑结束时项目均应可构建、可测试：

### M1：工程基础与数据接入

完成 Clang 20 工具链约束、CMake、核心数据类型、通用错误、配置加载、日志、SQLite 初始迁移和测试框架；完成文件发现、文件身份、CSV/JSONL 解析以及基础数据校验。

M1 验收时，项目应能使用示例配置启动，导入 CSV 和 JSONL 文件，将通过基础校验的记录写入数据库，并能正确隔离无法处理的文件。

### M2：二进制接入与时序处理

完成 TLM C11 编解码库、CRC 和损坏帧恢复；完成校准、单位换算、去重、乱序水位线、缺失检测、插值、固定窗口、滚动窗口、批量事务及文件检查点。

M2 验收时，三种格式应产生一致的标准化结果；进程中断后能够从检查点恢复；固定测试集的样本、缺失区间和聚合结果应与预期一致。

### M3：业务规则与服务接口

完成表达式解析、类型及单位检查、派生指标、异常规则状态机、事件合并与确认；完成 HTTP API、命令行工具、配置热加载、审计日志、数据保留和优雅停机。

M3 验收时，应能通过 API 和 CLI 查询设备、样本、聚合和事件；配置热加载失败不得影响旧配置；相同输入重放不得重复产生业务结果。

### M4：报表、性能与交付完善

完成 CSV/JSON 报表、运行指标、数据生成器、性能测试、模糊测试和两小时稳定性测试；补齐 README、架构说明、配置参考、API 文档、TLM 协议说明及验收记录，并完成全部验收修正。

M4 验收时，应通过 Clang 20 的 Release、Debug、AddressSanitizer 和 UndefinedBehaviorSanitizer 构建与测试，满足代码规模、覆盖率、吞吐、内存和稳定性要求，形成完整可交付版本。

## 17. 实现约定

- 需求中未明确的普通实现细节，由开发者选择合理方案，并在架构说明中记录。
- 若需求之间发生冲突，优先级依次为：数据一致性、安全与资源边界、功能正确性、性能、实现便利性。
- 不允许以空实现、固定返回值或仅覆盖示例输入的方式满足接口。
- 所有后台任务均需支持状态查询、错误报告和进程关停协调。
- 所有影响历史解释的配置项必须带版本保存，查询结果应能追溯到处理时的配置版本。
- 示例数据和测试数据不得依赖互联网下载，完整测试可在离线环境执行。
